{
  "name": "browse",
  "version": "1.0.0",
  "description": "Browser automation for visual testing and QA with Playwright",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "browse": "./cli.js"
  },
  "scripts": {
    "build": "tsc",
    "package": "npm run build && mkdir -p ../../dist-skills && tar -czf ../../dist-skills/browse.skill.tar.gz dist cli.js skill.json SKILL.md"
  },
  "keywords": ["openclaw", "browser", "screenshot", "playwright", "automation"],
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^12.0.0",
    "playwright": "^1.42.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
