{
  "name": "ship",
  "version": "1.0.0",
  "description": "One-command release pipeline - version bump, changelog, git tag, GitHub release",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ship": "./cli.js"
  },
  "scripts": {
    "build": "tsc",
    "package": "npm run build && mkdir -p ../../dist-skills && tar -czf ../../dist-skills/ship.skill.tar.gz dist cli.js skill.json SKILL.md"
  },
  "keywords": ["openclaw", "release", "version", "changelog", "github"],
  "dependencies": {
    "chalk": "^5.6.2",
    "semver": "^7.7.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/semver": "^7.5.6",
    "typescript": "^5.3.3"
  }
}
