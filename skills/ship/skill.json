{
  "name": "ship",
  "version": "1.0.0",
  "description": "One-command release pipeline with versioning, changelog, and GitHub releases",
  "entry": "cli.js",
  "bin": "superpowers ship",
  "requires": {
    "bins": ["node", "npx", "git"],
    "npm": ["@nko/superpowers"]
  },
  "env": {
    "GH_TOKEN": "GitHub personal access token with repo scope"
  }
}
