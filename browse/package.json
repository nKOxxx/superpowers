{
  "name": "@superpowers/browse",
  "version": "1.0.0",
  "description": "Browser automation skill for OpenClaw with Playwright",
  "main": "handler.ts",
  "scripts": {
    "build": "tsc handler.ts --outDir dist --esModuleInterop --target ES2020 --module commonjs --resolveJsonModule"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
