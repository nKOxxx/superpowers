{
  "name": "browse",
  "version": "1.0.0",
  "description": "Browser automation for visual testing and QA with Playwright",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "browse": "./cli.js"
  },
  "scripts": {
    "build": "tsc",
    "package": "npm run build && tar -czf ../dist-skills/browse.skill.tar.gz -C .. browse/dist browse/cli.js browse/skill.json"
  },
  "keywords": ["openclaw", "browser", "screenshot", "playwright", "automation"],
  "dependencies": {}
}
