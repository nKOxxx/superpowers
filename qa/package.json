{
  "name": "@superpowers/qa",
  "version": "1.0.0",
  "description": "QA testing skill for OpenClaw with multi-framework support",
  "main": "handler.ts",
  "scripts": {
    "build": "tsc handler.ts --outDir dist --esModuleInterop --target ES2020 --module commonjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
