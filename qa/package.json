{
  "name": "qa",
  "version": "1.0.0",
  "description": "Systematic testing as QA Lead - analyzes code changes and runs appropriate tests",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "qa": "./cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "package": "npm run build && mkdir -p ../dist-skills && tar -czf ../dist-skills/qa.skill.tar.gz dist cli.js skill.json SKILL.md package.json"
  },
  "keywords": ["openclaw", "testing", "qa", "vitest", "jest", "mocha"],
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^12.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
