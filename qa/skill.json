{
  "name": "qa",
  "version": "1.0.0",
  "description": "Systematic testing as QA Lead. Analyzes code changes and runs appropriate tests. Use when user needs code testing, quality assurance, regression testing, or test planning.",
  "author": "nKO",
  "entry": {
    "type": "node",
    "bin": "node",
    "args": ["dist/qa/index.js"]
  },
  "triggers": [
    "/qa",
    "run tests",
    "test coverage",
    "QA review",
    "quality check"
  ],
  "requires": {
    "bins": ["node", "npx", "git"],
    "npm": ["@nko/superpowers"]
  },
  "env": {},
  "config": {
    "schema": {
      "defaultMode": {
        "type": "string",
        "enum": ["targeted", "smoke", "full"],
        "default": "targeted"
      },
      "testCommand": {
        "type": "string",
        "default": "npm test"
      },
      "coverageThreshold": {
        "type": "number",
        "default": 80
      }
    }
  },
  "examples": [
    {
      "prompt": "/qa",
      "description": "Run targeted tests based on git diff"
    },
    {
      "prompt": "/qa --mode=smoke",
      "description": "Quick smoke test validation"
    },
    {
      "prompt": "/qa --mode=full --coverage",
      "description": "Full test suite with coverage"
    }
  ]
}
